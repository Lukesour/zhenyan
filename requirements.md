# 需求文档: 留学申请分析系统 MVP 核心功能改进

## 1. 简介



本文档旨在明确留学申请分析系统从当前基于模拟数据的原型，升级为由真实数据和大型语言模型驱动的核心 MVP (Minimum Viable Product) 所需的功能需求。核心目标是集成外部数据源 (Supabase)，引入AI分析能力 (Gemma 3)，并优化用户输入体验。



## 2. 功能特性



- **真实案例对标**: 系统将从 Supabase 数据库中获取真实的留学申请案例，为用户提供更准确的相似案例匹配和对比分析。
- **AI 驱动的文本分析**: 所有定性分析，包括优势、劣势、推荐理由等，将由 Google 的 Gemma 3 27b 大语言模型动态生成，取代现有的硬编码文本。
- **自动化数据填充**: 用户的院校层次将根据提供的学校名称自动从内部数据文件匹配，简化用户输入。
- **精简的用户输入**: 移除不必要的表单字段，并明确标识选填项，优化表单填写流程。

------



## 3. 需求列表





### 3.1. 后端集成





#### **需求 1: 集成 Supabase 数据库以获取真实案例**



- **用户故事**: 作为一个申请者，我希望能将我的背景与真实的成功案例进行比较，以便获得更现实、更有参考价值的定位分析。
- **验收标准 (EARS)**:
  - **R1.1**: **当**收到 `/api/analyze` 请求时，系统**应**查询 Supabase 数据库中的 `processed_cases` 表以获取所有历史案例数据。
  - **R1.2**: **如果** Supabase 数据库连接失败或查询不到任何数据，系统**应**优雅地降级，并可以在日志中记录该错误，同时使用一套固定的、最小化的模拟案例数据以完成分析流程，确保系统不会崩溃。
  - **R1.3**: 系统**应**使用从 Supabase 获取的真实案例数据，通过相似度匹配算法，为用户找出最相关的案例用于报告生成。



#### **需求 2: 集成 Gemma 3 大语言模型进行智能分析**



- **用户故事**: 作为一个申请者，我希望我的科研、实习等文字性描述能被先进的 AI 模型分析，以便得到更深入、更人性化的评估反馈。
- **验收标准 (EARS)**:
  - **R2.1**: **当**需要生成分析报告中的所有文本内容时（包括但不限于优势、劣势、综合评价、推荐理由、案例对比等），系统**应**调用 Gemma 3 27b 模型 API。
  - **R2.2**: 系统**应**根据用户的 `UserBackground` 信息，为每个分析项（如优势分析）动态构建结构化的提示词 (Prompt)。
  - **R2.3**: **如果** Gemma 3 API 调用失败或返回格式不正确，系统**应**能够捕获异常，并自动降级使用一套预设的、基于模板的文本生成逻辑来填充报告，以保证用户总能收到一份完整的报告。



### 3.2. 数据与逻辑调整





#### **需求 3: 自动化院校层次匹配**



- **用户故事**: 作为一个开发者，我希望系统能根据用户输入的院校名称自动匹配其层次等级，以便用户无需手动选择，从而保证数据的一致性和准确性。
- **验收标准 (EARS)**:
  - **R3.1**: **在**后端 `scoringService.ts` 中，系统**应**根据用户提交的 `university` 字段，查询 `data/university_tiers.json` 文件。
  - **R3.2**: **如果**在 `data/university_tiers.json` 中找到匹配的院校，系统**应**使用文件内定义的 `Tier` 等级进行后续评分计算。
  - **R3.3**: **如果**在 `data/university_tiers.json` 中找不到匹配的院校，系统**应**默认将其归类为 "Tier 4"。
  - **R3.4**: 前端表单 `FormPage.tsx` 中**应**移除 "院校等级" (`universityTier`) 这个 `Select` 选择框。



### 3.3. 前端用户体验优化





#### **需求 4: 精简学术背景表单**



- **用户故事**: 作为一个申请者，我只想填写我的具体专业名称，不想被一个模糊的 "专业大类" 搞糊涂，以便简化和加速表单填写过程。
- **验收标准 (EARS)**:
  - **R4.1**: 系统**应**从 `frontend/src/pages/FormPage.tsx` 的表单中彻底移除 "专业大类" (`majorCategory`) 字段及其相关的输入组件。
  - **R4.2**: 系统**应**更新 `frontend/src/types/index.ts` 中的 `UserBackground` 接口，将 `majorCategory` 字段标记为可选 (`majorCategory?: string;`)，因为这个值将由后端逻辑填充或不再需要。
  - **R4.3**: 后端接收到不含 `majorCategory` 的请求时，**不应**报错。



#### **需求 5: 明确标识可选表单项**



- **用户故事**: 作为一个申请者，我希望一眼就能看出哪些信息是必须填写的，哪些是选填的，以便我能快速完成核心信息的输入。
- **验收标准 (EARS)**:
  - **R5.1**: **在** `FormPage.tsx` 页面上，所有非必填的表单项（如语言成绩、标准化考试、各类经历等），其 `label` 旁边**应**明确展示 "(选填)" 或类似标识。
  - **R5.2**: 语言成绩和标准化考试的整个卡片（`Card`）内容都属于选填，用户即使不填写任何内容，也**应**能成功提交表单。
  - **R5.3**: 各类经历（科研、实习等）列表初始为空，用户不添加任何经历也**应**能成功提交表单。